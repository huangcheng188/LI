# STM32CubeMX + Keil MDK-ARM Project - .gitignore

# ============================================
# 编译输出文件（应该忽略）
# ============================================
# 目标文件和依赖文件
*.o
*.d
*.crf
*.dep

# 链接器输出
*.axf
*.hex
*.bin
*.elf
*.map
*.lst
*.htm
*.lnp
*.sct

# 编译日志
*.build_log.htm

# ============================================
# Keil MDK-ARM 用户特定文件（应该忽略）
# ============================================
# 用户界面设置（包含用户名、窗口位置等个人设置）
*.uvguix.*

# 用户选项文件（包含个人编译选项、窗口布局等）
*.uvoptx

# ============================================
# 临时文件和备份文件（应该忽略）
# ============================================
*.bak
*.tmp
*~
*.swp
*.swo

# ============================================
# 系统文件（应该忽略）
# ============================================
.DS_Store
Thumbs.db
desktop.ini

# ============================================
# 重要工程配置文件（必须保留，不能忽略）
# ============================================
# 使用 ! 来明确保留这些文件，即使它们可能匹配上面的忽略规则

# STM32CubeMX 配置文件（修改硬件配置后必须提交）
!LI.ioc
!.mxproject

# Keil MDK-ARM 工程文件（修改工程设置后必须提交）
!MDK-ARM/
!MDK-ARM/*.uvprojx

# 调试配置文件（修改调试设置后必须提交）
!MDK-ARM/DebugConfig/
!MDK-ARM/DebugConfig/*.dbgconf

# RTE 组件配置（修改组件配置后必须提交）
!MDK-ARM/RTE/
!MDK-ARM/RTE/**/*.h

# 启动文件（如果修改了启动代码，必须提交）
!MDK-ARM/startup_*.s

# 用户源代码（必须保留）
!Core/
!Core/**/*.c
!Core/**/*.h
!Core/**/*.s

# HAL 驱动库（建议保留，便于版本控制）
!Drivers/
!Drivers/**/*.c
!Drivers/**/*.h
!Drivers/**/*.s

# ============================================
# 重要提示：修改以下文件后必须提交到 Git
# ============================================
# 1. LI.ioc - 在 STM32CubeMX 中修改硬件配置（引脚、时钟、外设等）
# 2. MDK-ARM/*.uvprojx - 在 Keil 中修改工程设置（编译选项、链接脚本等）
# 3. MDK-ARM/DebugConfig/*.dbgconf - 修改调试配置
# 4. MDK-ARM/RTE/** - 修改 RTE 组件配置
# 5. Core/** - 所有用户代码修改
# 6. .mxproject - STM32CubeMX 项目元数据变更

